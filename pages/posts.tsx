import React from 'react';
import 'tailwindcss/tailwind.css'

interface IPostProps {
}

const Posts : React.FC<IAppProps> = (props: IPostProps) => {
  	return <div>
		Bài viết
  	</div>;
};

export default Posts;
